#ifndef TASKS_HPP
#define TASKS_HPP

namespace tasks
{
  bool choose_order(const char* order);
  void first_task(const char* order);
  void second_task(const char* file);
  void third_task();
  void fourth_task(const char* order, int size);
}

#endif // !TASKS_HPP
